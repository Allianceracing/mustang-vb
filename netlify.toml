# Netlify configuration

[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Make sure /api/content routes to the function
[[redirects]]
  from = "/api/content"
  to = "/.netlify/functions/content"
  status = 200
  force = true
